const config = {
    mysql: {
        user: '-',
        password: '-..',
        host: '--',
        port: '3307',
        dbname: 'iot',
    },
    mqtt: {
        host: 'mqtt://--',
        option: {
            username: '--',
            password: '--',
            port: '--',
        }
    }
}

module.exports = config;